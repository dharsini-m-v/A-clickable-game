/* =========================================================
   TOKENS
========================================================= */
:root{
  --red:#FF6B6B; --orange:#FFA53C; --yellow:#FFD93C;
  --green:#4ECB71; --blue:#3EC1F3; --purple:#B673FF; --pink:#FF8FD9;

  --font-display:'Baloo 2', system-ui, sans-serif;
  --font-body:'Nunito', system-ui, sans-serif;

  --radius-lg:28px;
  --radius-md:18px;
  --radius-sm:12px;

  --shadow-soft:0 10px 30px rgba(60,40,90,0.15);
  --transition-theme: background 1.1s ease, color .6s ease;
}

/* LIGHT THEME (sunrise) */
[data-theme="light"]{
  --sky-top:#FFE7B0; --sky-mid:#FFCFE0; --sky-bot:#BFEBFF;
  --sun-glow:#FFC24D;
  --ground:#8FE38A; --ground-dark:#6BC96A;
  --ink:#3A2E4D; --ink-soft:#5E4E78;
  --panel-bg: rgba(255,255,255,0.62);
  --panel-border: rgba(255,255,255,0.85);
  --card-bg: rgba(255,255,255,0.75);
  --nav-bg: rgba(255,255,255,0.55);
  --accent-strong:#3A2E4D;
}

/* DARK THEME (crescent night) */
[data-theme="dark"]{
  --sky-top:#0B1030; --sky-mid:#241B4D; --sky-bot:#3B2A6B;
  --sun-glow:#E8E3FF;
  --ground:#1F2A44; --ground-dark:#141C30;
  --ink:#F3EFFF; --ink-soft:#C9BFEE;
  --panel-bg: rgba(30,22,58,0.6);
  --panel-border: rgba(255,255,255,0.12);
  --card-bg: rgba(40,30,70,0.65);
  --nav-bg: rgba(20,14,40,0.55);
  --accent-strong:#FFD93C;
}

*{ box-sizing:border-box; }
html,body{ height:100%; }
body{
  margin:0;
  font-family:var(--font-body);
  color:var(--ink);
  background:linear-gradient(180deg,var(--sky-top),var(--sky-mid) 55%,var(--sky-bot));
  transition:var(--transition-theme);
  overflow-x:hidden;
  cursor:none;
}
h1,h2,h3{ font-family:var(--font-display); margin:0; }
button{ font-family:var(--font-body); cursor:none; border:none; }

/* restore normal cursor on inputs so typing still feels right */
input, textarea{ cursor:auto; }

/* =========================================================
   SPARKLE CURSOR
========================================================= */
#sparkle-canvas{
  position:fixed; inset:0; width:100vw; height:100vh;
  pointer-events:none; z-index:9999;
}

/* =========================================================
   TOP NAV
========================================================= */
.topnav{
  position:fixed; top:14px; left:14px; right:14px;
  display:flex; align-items:center; gap:10px;
  z-index:500;
}
.nav-pill{
  background:var(--nav-bg);
  backdrop-filter: blur(10px);
  border:1px solid var(--panel-border);
  color:var(--ink);
  padding:10px 16px;
  border-radius:999px;
  font-weight:700;
  font-size:0.95rem;
  box-shadow:var(--shadow-soft);
  transition:transform .15s ease;
}
.nav-pill:hover{ transform:translateY(-2px) scale(1.04); }
.nav-pill:active{ transform:scale(0.92); }
.nav-spacer{ flex:1; }
.theme-toggle{
  width:46px; height:46px; border-radius:50%;
  background:var(--nav-bg);
  backdrop-filter: blur(10px);
  border:1px solid var(--panel-border);
  font-size:1.3rem;
  box-shadow:var(--shadow-soft);
  transition:transform .3s ease;
}
.theme-toggle:hover{ transform:rotate(20deg) scale(1.1); }
.theme-toggle:active{ transform:scale(0.85) rotate(90deg); }

/* =========================================================
   SCREENS
========================================================= */
.screen{
  display:none;
  min-height:100vh;
  width:100%;
  position:relative;
  padding:90px 20px 60px;
}
.screen.active{ display:block; animation:fadeIn .5s ease; }
@keyframes fadeIn{ from{opacity:0; transform:translateY(12px);} to{opacity:1; transform:translateY(0);} }

.screen-title{
  text-align:center;
  font-size:clamp(1.6rem,4vw,2.4rem);
  color:var(--ink);
  margin-bottom:34px;
  text-shadow:0 2px 0 rgba(255,255,255,0.3);
}

/* =========================================================
   HOME SCREEN — SKY
========================================================= */
#screen-home{ min-height:100vh; padding:0; overflow:hidden; }
.sky{ position:absolute; inset:0; z-index:0; overflow:hidden; }

.sun-moon{
  position:absolute; top:8%; right:12%;
  width:110px; height:110px; border-radius:50%;
  background:radial-gradient(circle at 35% 35%, #FFF6D8, var(--sun-glow));
  box-shadow:0 0 60px 20px var(--sun-glow);
  transition: box-shadow 1s ease, background 1s ease, clip-path .8s ease;
  animation: float 6s ease-in-out infinite;
}
[data-theme="dark"] .sun-moon{
  background: radial-gradient(circle at 30% 30%, #FFFFFF, #DCD6FF);
  clip-path: polygon(45% 0%, 100% 0, 100% 100%, 45% 100%, 65% 50%);
  box-shadow:0 0 40px 12px rgba(232,227,255,0.55);
}
@keyframes float{ 0%,100%{transform:translateY(0);} 50%{transform:translateY(-14px);} }

.cloud{
  position:absolute; background:#fff; border-radius:50px;
  opacity:0.9; filter:drop-shadow(0 6px 6px rgba(0,0,0,0.08));
  transition: opacity .8s ease, background .8s ease;
}
[data-theme="dark"] .cloud{ background:#2B2456; opacity:0.5; }
.cloud::before, .cloud::after{ content:""; position:absolute; background:inherit; border-radius:50%; }
.c1{ width:120px; height:36px; top:14%; left:8%; animation:driftRight 40s linear infinite; }
.c1::before{ width:60px; height:60px; top:-30px; left:10px; }
.c1::after{ width:44px; height:44px; top:-20px; left:60px; }
.c2{ width:90px; height:28px; top:26%; left:60%; animation:driftRight 55s linear infinite; }
.c2::before{ width:44px; height:44px; top:-22px; left:8px; }
.c2::after{ width:34px; height:34px; top:-14px; left:44px; }
.c3{ width:70px; height:22px; top:10%; left:38%; animation:driftRight 65s linear infinite; }
.c3::before{ width:34px; height:34px; top:-16px; left:8px; }
@keyframes driftRight{ from{transform:translateX(-140px);} to{transform:translateX(120vw);} }

.stars{
  position:absolute; inset:0; opacity:0; transition:opacity 1s ease;
  background-image:
    radial-gradient(2px 2px at 10% 20%, #fff, transparent),
    radial-gradient(2px 2px at 30% 10%, #fff, transparent),
    radial-gradient(1.5px 1.5px at 50% 25%, #fff, transparent),
    radial-gradient(2px 2px at 70% 8%, #fff, transparent),
    radial-gradient(1.5px 1.5px at 85% 18%, #fff, transparent),
    radial-gradient(2px 2px at 20% 35%, #fff, transparent),
    radial-gradient(1.5px 1.5px at 60% 30%, #fff, transparent),
    radial-gradient(2px 2px at 92% 32%, #fff, transparent);
  animation:twinkle 3s ease-in-out infinite;
}
[data-theme="dark"] .stars{ opacity:1; }
@keyframes twinkle{ 0%,100%{opacity:0.6;} 50%{opacity:1;} }

.rainbow-arc{
  position:absolute; left:50%; bottom:22%; transform:translateX(-50%);
  width:900px; height:450px; max-width:160vw;
  border-radius:50% 50% 0 0;
  box-shadow:
    0 0 0 6px var(--red) inset,
    0 0 0 16px var(--orange) inset,
    0 0 0 26px var(--yellow) inset,
    0 0 0 36px var(--green) inset,
    0 0 0 46px var(--blue) inset,
    0 0 0 56px var(--purple) inset;
  opacity:0.85;
  clip-path: inset(0 0 50% 0);
}

/* =========================================================
   PLAYGROUND
========================================================= */
.playground{ position:absolute; inset:0; z-index:1; }
.hill{
  position:absolute; left:-10%; right:-10%; bottom:0; height:32%;
  background:linear-gradient(180deg, var(--ground), var(--ground-dark));
  border-radius:50% 50% 0 0 / 100px 100px 0 0;
  transition: background 1s ease;
}
.tree{ position:absolute; bottom:26%; font-size:4.5rem; filter:drop-shadow(0 6px 4px rgba(0,0,0,0.15)); animation: sway 5s ease-in-out infinite; }
.t1{ left:4%; } .t2{ right:5%; animation-delay:1s; }
@keyframes sway{ 0%,100%{transform:rotate(-3deg);} 50%{transform:rotate(3deg);} }

.flowers{
  position:absolute; bottom:24%; left:0; right:0; text-align:center;
  font-size:1.8rem; letter-spacing:26px; opacity:0.95;
}

.seesaw{ position:absolute; bottom:25%; left:44%; width:170px; height:60px; }
.seesaw-fulcrum{
  position:absolute; bottom:0; left:50%; transform:translateX(-50%);
  width:0; height:0; border-left:22px solid transparent; border-right:22px solid transparent;
  border-bottom:34px solid var(--purple);
}
.seesaw-bar{
  position:absolute; bottom:30px; left:0; width:170px; height:14px; border-radius:10px;
  background:var(--red); transform-origin:center; animation:seesaw 3.2s ease-in-out infinite;
}
@keyframes seesaw{ 0%,100%{transform:rotate(-10deg);} 50%{transform:rotate(10deg);} }

.kite{
  position:absolute; top:16%; left:60%; font-size:3rem;
  animation:kiteFloat 4.5s ease-in-out infinite;
  filter:drop-shadow(0 6px 5px rgba(0,0,0,0.2));
}
@keyframes kiteFloat{
  0%{ transform:translate(0,0) rotate(-6deg); }
  25%{ transform:translate(-14px,-18px) rotate(4deg); }
  50%{ transform:translate(6px,-30px) rotate(-4deg); }
  75%{ transform:translate(-8px,-10px) rotate(6deg); }
  100%{ transform:translate(0,0) rotate(-6deg); }
}
.kite-string{
  position:absolute; top:26%; left:41%; width:2px; height:170px;
  background:rgba(80,60,40,0.5); transform:rotate(18deg); transform-origin:top;
}

.runner{
  position:absolute; bottom:24%; font-size:3.4rem;
  filter:drop-shadow(0 6px 4px rgba(0,0,0,0.2));
}
.boy{ animation: runAcross 9s linear infinite; }
.girl{ animation: runAcross 9s linear infinite; animation-delay:0.6s; }
@keyframes runAcross{
  0%{ left:-10%; transform:scaleX(1) translateY(0); }
  48%{ left:78%; transform:scaleX(1) translateY(-4px); }
  50%{ left:80%; transform:scaleX(-1); }
  98%{ left:-8%; transform:scaleX(-1) translateY(-4px); }
  100%{ left:-10%; transform:scaleX(1); }
}

/* =========================================================
   HOME CONTENT
========================================================= */
.home-content{
  position:relative; z-index:5;
  display:flex; flex-direction:column; align-items:center;
  justify-content:center; text-align:center;
  min-height:100vh; padding-bottom:8%;
}
.title{
  font-size:clamp(2.2rem, 6vw, 4rem);
  font-weight:800;
  background:linear-gradient(90deg,var(--red),var(--orange),var(--yellow),var(--green),var(--blue),var(--purple));
  -webkit-background-clip:text; background-clip:text; color:transparent;
  text-shadow:none;
  animation: hueDrift 8s linear infinite;
  filter: drop-shadow(0 4px 10px rgba(0,0,0,0.12));
}
@keyframes hueDrift{ 0%{filter:hue-rotate(0deg) drop-shadow(0 4px 10px rgba(0,0,0,0.12));} 100%{filter:hue-rotate(360deg) drop-shadow(0 4px 10px rgba(0,0,0,0.12));} }
.subtitle{ margin-top:14px; font-size:1.15rem; color:var(--ink-soft); font-weight:700; }

.cta-button{
  margin-top:34px;
  padding:18px 52px;
  font-family:var(--font-display);
  font-size:1.3rem; font-weight:700; letter-spacing:1px;
  color:#fff;
  background: var(--accent-strong);
  border-radius:999px;
  box-shadow:0 12px 24px rgba(0,0,0,0.2);
  transition: transform .18s ease, box-shadow .18s ease;
}
[data-theme="dark"] .cta-button{ color:#241B4D; box-shadow:0 0 30px 6px rgba(232,227,255,0.5); }
.cta-button:hover{ transform:translateY(-4px) scale(1.05); }
.cta-button.bounce{ animation: bounceClick .5s ease; }
@keyframes bounceClick{
  0%{ transform:scale(1); }
  30%{ transform:scale(0.88) translateY(4px); }
  60%{ transform:scale(1.12) translateY(-10px); }
  100%{ transform:scale(1); }
}

/* =========================================================
   CATEGORY CARDS
========================================================= */
.category-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(210px,1fr));
  gap:26px; max-width:920px; margin:0 auto;
}
.category-card{
  position:relative;
  background:var(--card-bg);
  backdrop-filter: blur(12px);
  border:1px solid var(--panel-border);
  border-radius:var(--radius-lg);
  padding:38px 20px;
  display:flex; flex-direction:column; align-items:center; gap:14px;
  box-shadow:var(--shadow-soft);
  transition: transform .2s ease, box-shadow .2s ease;
  overflow:hidden;
}
.category-card::before{
  content:""; position:absolute; inset:0; border-radius:inherit;
  background: linear-gradient(135deg, var(--card-color), transparent 70%);
  opacity:0.18; pointer-events:none;
}
.category-card:hover{ transform:translateY(-8px) scale(1.03); box-shadow:0 18px 34px rgba(0,0,0,0.2); }
.category-card:active, .category-card.bounce{ animation:bounceClick .5s ease; }
.card-emoji{ font-size:3.6rem; filter:drop-shadow(0 6px 6px rgba(0,0,0,0.15)); }
.card-label{ font-family:var(--font-display); font-size:1.3rem; font-weight:700; color:var(--accent-strong); }

.secondary-button{
  display:block; margin:44px auto 0;
  padding:14px 30px;
  background:var(--card-bg); backdrop-filter:blur(10px);
  border:1px solid var(--panel-border);
  border-radius:999px; font-weight:800; font-size:1.05rem; color:var(--ink);
  box-shadow:var(--shadow-soft);
  transition:transform .18s ease;
}
.secondary-button:hover{ transform:translateY(-3px) scale(1.03); }
.secondary-button:active{ transform:scale(0.94); }

/* =========================================================
   ITEM GRID + POPUP
========================================================= */
.item-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(180px,1fr));
  gap:24px; max-width:900px; margin:0 auto;
}
.item-card{
  background:var(--card-bg); backdrop-filter:blur(10px);
  border:1px solid var(--panel-border); border-radius:var(--radius-md);
  padding:16px; text-align:center; box-shadow:var(--shadow-soft);
  transition:transform .18s ease;
}
.item-card:hover{ transform:translateY(-6px) scale(1.03); }
.item-card:active{ animation:bounceClick .45s ease; }
.item-card img{
  width:100%; aspect-ratio:1/1; object-fit:cover; border-radius:var(--radius-sm);
  box-shadow:0 6px 16px rgba(0,0,0,0.15);
}
.item-card .item-name{ margin-top:10px; font-family:var(--font-display); font-weight:700; color:var(--ink); }

.item-popup{
  position:fixed; inset:0; z-index:800;
  display:none; align-items:center; justify-content:center;
  background:rgba(20,10,40,0.45); backdrop-filter:blur(6px);
}
.item-popup.active{ display:flex; animation:fadeIn .3s ease; }
.item-popup-card{
  position:relative;
  background:var(--card-bg); backdrop-filter:blur(16px);
  border:1px solid var(--panel-border); border-radius:var(--radius-lg);
  padding:30px; max-width:420px; width:88%;
  text-align:center; box-shadow:0 24px 60px rgba(0,0,0,0.35);
  transform:scale(0.6); opacity:0;
  animation:popIn .45s cubic-bezier(.2,1.4,.4,1) forwards;
}
@keyframes popIn{ to{ transform:scale(1); opacity:1; } }
.item-popup-card img{
  width:100%; max-height:320px; object-fit:cover; border-radius:var(--radius-md);
  box-shadow:0 10px 24px rgba(0,0,0,0.25);
}
.popup-name{ margin-top:16px; font-family:var(--font-display); font-size:1.7rem; font-weight:800; color:var(--accent-strong); }
.popup-close{
  position:absolute; top:-14px; right:-14px; width:38px; height:38px; border-radius:50%;
  background:var(--red); color:#fff; font-weight:800; box-shadow:var(--shadow-soft);
}

/* =========================================================
   IMAGE EDITOR
========================================================= */
.upload-area{
  max-width:520px; margin:0 auto; padding:50px 24px;
  border:3px dashed var(--panel-border);
  border-radius:var(--radius-lg);
  background:var(--card-bg); backdrop-filter:blur(10px);
  text-align:center; box-shadow:var(--shadow-soft);
  transition: border-color .2s ease, transform .15s ease;
}
.upload-area.drag-over{ border-color:var(--blue); transform:scale(1.02); }
.upload-emoji{ font-size:3rem; margin:0 0 6px; }
.upload-button{
  display:inline-block; margin-top:14px; padding:12px 28px;
  background:var(--accent-strong); color:#fff; border-radius:999px;
  font-weight:800; cursor:none; transition:transform .15s ease;
}
[data-theme="dark"] .upload-button{ color:#241B4D; }
.upload-button:hover{ transform:scale(1.05); }
.upload-hint{ font-size:0.85rem; color:var(--ink-soft); margin-top:10px; }

.editor-workspace{
  display:grid; grid-template-columns:270px 1fr; gap:22px;
  max-width:1100px; margin:0 auto;
}
.hidden{ display:none !important; }

.editor-panel{
  background:var(--panel-bg); backdrop-filter:blur(12px);
  border:1px solid var(--panel-border); border-radius:var(--radius-lg);
  padding:18px; max-height:76vh; overflow-y:auto;
  display:flex; flex-direction:column; gap:18px;
}
.panel-group h3{
  font-size:0.95rem; text-transform:uppercase; letter-spacing:1px;
  color:var(--ink-soft); margin-bottom:10px;
}
.tool-btn{
  display:block; width:100%; text-align:left;
  background:var(--card-bg); color:var(--ink);
  border:1px solid var(--panel-border);
  border-radius:var(--radius-sm);
  padding:9px 12px; margin-bottom:7px; font-weight:700; font-size:0.92rem;
  transition:transform .12s ease, background .15s ease;
}
.tool-btn:hover{ transform:translateX(3px); background: var(--nav-bg); }
.tool-btn:active, .tool-btn.bounce{ animation:bounceClick .4s ease; }
.tool-btn.primary{ background:var(--green); color:#123; }
.tool-btn.danger{ background:var(--red); color:#fff; }
.tool-btn.small{ display:inline-block; width:auto; margin-left:6px; }
.tool-inline{ display:flex; align-items:center; gap:6px; flex-wrap:wrap; margin-top:4px; }
.mini-input{
  width:56px; padding:7px; border-radius:8px; border:1px solid var(--panel-border);
  background:var(--card-bg); color:var(--ink);
}
.tool-slider{ margin:10px 0; }
.tool-slider label{ display:flex; justify-content:space-between; font-size:0.85rem; font-weight:700; color:var(--ink-soft); }
.tool-slider input[type=range]{ width:100%; accent-color:var(--purple); }

.editor-preview{
  background:var(--panel-bg); backdrop-filter:blur(12px);
  border:1px solid var(--panel-border); border-radius:var(--radius-lg);
  padding:18px; display:flex; flex-direction:column; align-items:center; gap:12px;
}
.preview-info{ display:flex; justify-content:space-between; width:100%; font-size:0.85rem; color:var(--ink-soft); font-weight:700; }
.compare-toggle{ display:flex; gap:8px; }
.compare-btn{
  padding:6px 16px; border-radius:999px; font-weight:700; font-size:0.85rem;
  background:var(--card-bg); color:var(--ink-soft); border:1px solid var(--panel-border);
}
.compare-btn.active{ background:var(--accent-strong); color:#fff; }
[data-theme="dark"] .compare-btn.active{ color:#241B4D; }

.preview-frame{
  width:100%; height:420px; border-radius:var(--radius-md);
  background: repeating-conic-gradient(#ffffff22 0% 25%, transparent 0% 50%) 50% / 22px 22px;
  overflow:auto; display:flex; align-items:center; justify-content:center;
  border:1px solid var(--panel-border);
}
.preview-frame img{
  max-width:100%; max-height:100%; transition:transform .2s ease; border-radius:8px;
}
.zoom-controls{ display:flex; align-items:center; gap:8px; }
.zoom-btn{
  background:var(--card-bg); border:1px solid var(--panel-border); border-radius:8px;
  padding:6px 12px; font-weight:700;
}
#zoomLabel{ font-weight:700; min-width:52px; text-align:center; }

/* =========================================================
   RESPONSIVE
========================================================= */
@media (max-width: 820px){
  .editor-workspace{ grid-template-columns:1fr; }
  .editor-panel{ max-height:none; }
  .preview-frame{ height:300px; }
  .flowers{ letter-spacing:12px; font-size:1.4rem; }
  .runner{ font-size:2.4rem; }
  .kite{ font-size:2.2rem; }
}
@media (max-width: 480px){
  .topnav{ flex-wrap:wrap; }
  .title{ font-size:2rem; }
  .category-grid{ grid-template-columns:1fr 1fr; }
}

/* respect reduced motion */
@media (prefers-reduced-motion: reduce){
  *{ animation-duration:0.001ms !important; animation-iteration-count:1 !important; transition-duration:0.001ms !important; }
}
